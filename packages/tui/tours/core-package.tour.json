{
  "schemaVersion": 1,
  "id": "core-package",
  "title": "Core Package Overview",
  "intent": "A short dummy tour of the core package using its package.json.",
  "description": "Shows the core package role as the normalization layer above validated tour JSON.",
  "repo": {
    "vcs": "git",
    "rootHint": "TourGuide repository root"
  },
  "topics": [
    {
      "title": "1. Package",
      "steps": [
        {
          "title": "Core package metadata",
          "body": "`@tourguide/core` sits above schema and provides normalized tour data structures.",
          "presentation": {
            "kind": "single"
          },
          "anchors": [
            {
              "kind": "fileRange",
              "path": "packages/core/package.json",
              "range": {
                "startLine": 1,
                "endLine": 24
              },
              "note": "The dependency on `@tourguide/schema` shows core builds on validated tour artifacts."
            }
          ]
        }
      ]
    }
  ]
}
